{
  "name": "express-stripe-auth-skeleton",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API + demo. Run commands from api/ or via this wrapper.",
  "scripts": {
    "dev": "cd api && npm run dev",
    "build": "cd api && npm run build",
    "start": "cd api && npm start",
    "test": "cd api && npm test",
    "lint": "cd api && npm run lint",
    "db:migrate": "cd api && npm run db:migrate",
    "db:seed": "cd api && npm run db:seed"
  },
  "engines": {
    "node": ">=18"
  }
}
